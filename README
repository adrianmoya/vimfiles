Mi configuracion de vim

Para instalar:
cd ~
git clone git://github.com/adrianmoya/vimfiles.git .vim
ln -s ~/.vim/vimrc ~/.vimrc
cd .vim/
git submodule init
git submodule update

Requerimientos:
apt-get install exuberant-ctags
